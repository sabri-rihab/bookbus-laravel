php artisan make:migration create_city_table --create=city
        Schema::create('city', function (Blueprint $table) {
            $table->id();
            $table->string('name')->unique()->nullable();
            // $table->timestamps();
        });

write the attributs
php artisan migrate


create a sseder for our table => php artisan make:seeder citySeeder
